## 1. Egress

- [x] 1.1 `egress-proxy.mjs` in the runner image
- [x] 1.2 `NetworkConfig` (allowlist/open, allow list, internal network name)
- [x] 1.3 `Egress` context manager used by bouts and judge runs
- [x] 1.4 `egress` summary in record.json; network config in lock and bout IDs
- [x] 1.5 podman integration test (allowed, proxied-denied, direct-blocked)
- [x] 1.6 live smoke run through the proxy
