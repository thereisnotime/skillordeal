You are auditing the repository in the current directory ({arena}, primarily {language}) for security vulnerabilities. Scope: {scope}.

Work read-only. Investigate the code yourself; do not guess from file names. Report only issues you can point to in the code, with the exact file path (relative to the repository root) and line numbers.

For each finding give a title, category, CWE if one applies, severity, your confidence, the location, a description of how it can be exploited or why it matters, the evidence (code or data flow), and a recommendation.

When you are done, return the result in the required structured format. If you find nothing, return an empty findings list and say what you covered in the summary.
